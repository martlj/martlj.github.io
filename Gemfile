source "https://rubygems.org"

# This matches the Jekyll/plugin versions GitHub Pages uses in production,
# so your site builds the same way locally as it does on GitHub.
gem "github-pages", group: :jekyll_plugins
